### ls

```shell
ls <dir>	# 列出<dir>目录下的所有内容，缺省值为当前目录
	-a	# 显示所有文件，包括隐藏文件
	-l	# 以长格式显示当前目录中的文件和目录
	-lh	# 以人类可读的方式显示当前目录中的文件和目录大小
	-t	# 按照修改时间排序显示当前目录中的文件和目录
	-R	# 递归显示当前目录中的所有文件和子目录
ls -l /etc/passwd        # 显示/etc/passwd文件的详细信息
```
